directory to hold all of the user uploads.

The sub-directories are dedicated for the file type that user uploads.

each uploaded file is sorted and moved to the specific directory automatically after verification.
